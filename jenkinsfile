pipeline{
    agent any 
    tools {
        nodejs 'nodejs'
    }
    stages {
        stage ('run'){
            step{
                sh 'npm install'
            }
        }
        stage ('build'){
            step{
                sh 'npm test' 
                 echo "No tests defined"
            }
        }
        stage('run') {
            step('build'){
                sh 'npm run build'  
            }
        }
    }
}
